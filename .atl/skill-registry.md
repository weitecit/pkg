# Skill Registry — pkg

> Auto-generated by sdd-init. Do not edit manually.

## Project Standards (auto-resolved)

### go-testing (user skill)
- Trigger: Go tests, `go test`, coverage, Bubbletea teatest, golden files
- Path: `~/.config/opencode/skills/go-testing/SKILL.md`
- Compact rules:
  - Prefer table-driven tests; use `t.Run(tt.name, ...)`.
  - Test behavior and state transitions, not implementation trivia.
  - Use `t.TempDir()` for filesystem tests; never rely on a real home directory.
  - Keep integration tests skippable with `testing.Short()`.
  - For Bubbletea, test `Model.Update()` directly; use `teatest` only for interactive flows.
  - Golden files must be deterministic; update only via repo's `-update` path.
  - Use small mocks/interfaces around system or command execution boundaries.

### work-unit-commits (user skill)
- Trigger: implementation, commit splitting, chained PRs, keeping tests with code
- Path: `~/.config/opencode/skills/work-unit-commits/SKILL.md`
- Compact rules:
  - Commit by work unit (deliverable behavior), not by file type.
  - Keep tests in the same commit as the behavior they verify.
  - Keep docs with the user-visible change they explain.
  - Each commit must have one clear purpose and allow rollback without unrelated reverts.
  - If SDD tasks forecast >400-line change, group commits into chained PR slices.

### branch-pr (user skill)
- Trigger: creating, opening, or preparing PRs for review
- Path: `~/.config/opencode/skills/branch-pr/SKILL.md`
- Compact rules:
  - Every PR MUST link an approved issue with `status:approved` label.
  - Every PR MUST have exactly one `type:*` label.
  - Branch names MUST match `^(feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert)/[a-z0-9._-]+$`.
  - Conventional commits required: `type(scope): description`.
  - No `Co-Authored-By` trailers.

### chained-pr (user skill)
- Trigger: PRs over 400 lines, stacked PRs, review slices
- Path: `~/.config/opencode/skills/chained-pr/SKILL.md`
- Compact rules:
  - Split oversized changes into chained PRs protecting review focus.
  - Each slice must be independently reviewable and deployable.

### issue-creation (user skill)
- Trigger: creating GitHub issues, bug reports, feature requests
- Path: `~/.config/opencode/skills/issue-creation/SKILL.md`
- Compact rules:
  - Issue-first approach: issues before PRs.

### comment-writer (user skill)
- Trigger: PR feedback, issue replies, reviews, Slack messages, GitHub comments
- Path: `~/.config/opencode/skills/comment-writer/SKILL.md`
- Compact rules:
  - Write warm, direct collaboration comments.

### judgment-day (user skill)
- Trigger: judgment day, dual review, adversarial review
- Path: `~/.config/opencode/skills/judgment-day/SKILL.md`
- Compact rules:
  - Run blind dual review, fix confirmed issues, then re-judge.

### cognitive-doc-design (user skill)
- Trigger: writing guides, READMEs, RFCs, onboarding, architecture docs
- Path: `~/.config/opencode/skills/cognitive-doc-design/SKILL.md`
- Compact rules:
  - Design docs that reduce cognitive load.

### skill-creator (user skill)
- Trigger: new skills, agent instructions, documenting AI usage patterns
- Path: `~/.config/opencode/skills/skill-creator/SKILL.md`
- Compact rules:
  - Create LLM-first skills with valid frontmatter.

### Project Convention: AGENTS.md (user-level)
- Path: `~/.config/opencode/AGENTS.md`
- Compact rules:
  - Never add "Co-Authored-By" or AI attribution to commits.
  - Never build after changes.
  - When asking a question, STOP and wait for response.
  - Never agree with user claims without verification — say "dejame verificar" first.
  - Always propose alternatives with tradeoffs.
  - Verify technical claims before stating them.
  - Go tests / Bubbletea TUI testing → load go-testing skill.
  - Creating new AI skills → load skill-creator skill.

## Skill Trigger Table

| Context | Skill to Load |
|---------|---------------|
| Go tests, go test coverage, Bubbletea teatest | go-testing |
| Creating, opening, or preparing PRs | branch-pr |
| PRs over 400 lines, stacked PRs | chained-pr |
| Commit splitting, work-unit commits | work-unit-commits |
| Creating GitHub issues | issue-creation |
| PR feedback, reviews, comments | comment-writer |
| Dual review, adversarial review | judgment-day |
| Writing docs, READMEs, RFCs | cognitive-doc-design |
| Creating new AI skills | skill-creator |
| Swagger docs in Weitec | weitec-swagger |
